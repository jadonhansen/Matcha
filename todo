ummm
work...

Gabriel:
    - start using .env
    - add the rest of the search variables and learn to get them to render onto the page
    - make fields secure
    - handle errors
    - save location on login that was found

Jadon:
    - js function when switch is changed for location setting //this woulda confused me a bit if you hadnt put your name there bro XD <-- XDXDXD
	- if user has location pref set to off: (currently to always display that its on)
		do this:     input#location.custom-control-input(type="checkbox", name="location")